# Example environment configuration for the Luna backend

# -------------------- UAZAPI (WhatsApp) --------------------
# Base URL da sua instância Uazapi (sem barra final)
# Ex.: https://sua-instancia.uazapi.com
UAZAPI_BASE_URL=
# Token da INSTÂNCIA (não é o admin token)
UAZAPI_TOKEN=
# Endpoints opcionais (deixe como está se usar o padrão)
UAZAPI_SEND_TEXT_PATH=/send/text
UAZAPI_SEND_MEDIA_PATH=/send/media

# -------------------- WEBHOOK --------------------
# URL pública do seu backend (Railway → Deployments → Domains)
PUBLIC_BASE_URL=
# Prefixo onde o webhook ficará exposto (sem barra final). Ex.: /webhook/whatsapp
WEBHOOK_PATH=/webhook/whatsapp
# Token de verificação do webhook. O Uazapi deve incluí-lo como parâmetro ?token=...
WEBHOOK_VERIFY_TOKEN=

# -------------------- Postgres --------------------
# Aceita qualquer formato comum; o backend converte para o driver assíncrono:
# - postgres://user:pass@host:5432/dbname
# - postgresql://user:pass@host:5432/dbname
# - postgresql+psycopg2://user:pass@host:5432/dbname
# - postgresql+asyncpg://user:pass@host:5432/dbname
DATABASE_URL=
# SSL no asyncpg:
#   require (padrão)        -> usa CA do sistema (precisa do pacote ca-certificates na imagem)
#   disable                 -> sem TLS (apenas dev/local)
#   require_noverify        -> TLS sem verificação de certificado/hostname (use só se necessário)
#   verify-ca / verify-full -> informar DB_SSLROOTCERT com o CA PEM
DB_SSLMODE=require
# Caminho para o arquivo da CA quando usar verify-ca / verify-full (ex.: /app/certs/root.crt)
DB_SSLROOTCERT=

# -------------------- OpenAI --------------------
OPENAI_API_KEY=
ASSISTANT_ID=

# -------------------- Baserow (opcional para guardar arquivos) --------------------
# Cloud oficial: https://api.baserow.io
BASEROW_BASE_URL=
BASEROW_API_TOKEN=
