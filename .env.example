# Example environment configuration for the Luna backend

# ——————————— UAZAPI (WhatsApp) ———————————
UAZAPI_BASE_URL=
UAZAPI_TOKEN=
UAZAPI_SEND_TEXT_PATH=/send/text
UAZAPI_SEND_MEDIA_PATH=/send/media

# ——————————— WEBHOOK ———————————
# URL pública do seu backend (Railway -> "Domains"). Usado só para log no startup.
PUBLIC_BASE_URL=
# Prefixo onde o webhook ficará exposto (sem barra final). Ex.: /webhook/whatsapp
WEBHOOK_PATH=/webhook/whatsapp
# Token de verificação do webhook (compartilhe com o provedor). Aceitamos:
#   - Query:  ?token=SEU_TOKEN
#   - Header: X-Webhook-Token: SEU_TOKEN
#   - Meta-style (GET): hub.verify_token=SEU_TOKEN (responde hub.challenge)
WEBHOOK_VERIFY_TOKEN=

# ——————————— Postgres ———————————
DATABASE_URL=
# SSL (asyncpg):
#   require (padrão)        -> usa CA do sistema (ca-certificates)
#   disable                 -> sem TLS (dev/local)
#   require_noverify        -> TLS sem verificação (use só se necessário)
#   verify-ca / verify-full -> informar DB_SSLROOTCERT com o CA PEM
DB_SSLMODE=require
DB_SSLROOTCERT=

# ——————————— OpenAI ———————————
OPENAI_API_KEY=
ASSISTANT_ID=

# ——————————— Baserow (arquivos) ———————————
# Cloud oficial: https://api.baserow.io
BASEROW_BASE_URL=
BASEROW_API_TOKEN=
