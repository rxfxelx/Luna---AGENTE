# Example environment configuration for the Luna backend
#
# Copie para .env e preencha seus valores reais. Em produção (Railway),
# você pode definir as variáveis pelo painel do serviço.

# ——————————— UAZAPI (WhatsApp) ———————————
# Base URL da sua instância Uazapi (sem barra final)
# Exemplo: https://seu-dominio.uazapi.com
UAZAPI_BASE_URL=
# Token da INSTÂNCIA (não é o admin token)
UAZAPI_TOKEN=
# Endpoints opcionais (deixe como está se usar o padrão)
UAZAPI_SEND_TEXT_PATH=/send/text
UAZAPI_SEND_MEDIA_PATH=/send/media

# ——————————— Postgres ———————————
# Você pode colar aqui qualquer formato comum e o código converte para
# o driver assíncrono. Exemplos aceitos:
# - postgres://user:pass@host:5432/dbname
# - postgresql://user:pass@host:5432/dbname
# - postgresql+psycopg2://user:pass@host:5432/dbname
# - postgresql+asyncpg://user:pass@host:5432/dbname
DATABASE_URL=
# Para Railway normalmente 'require'. Em desenvolvimento local use 'disable'.
DB_SSLMODE=require

# ——————————— OpenAI ———————————
OPENAI_API_KEY=
ASSISTANT_ID=

# ——————————— Baserow (opcional para guardar arquivos) ———————————
# Se usar a cloud oficial, use https://api.baserow.io
# Se for self-hosted, aponte para sua URL (ex: https://meu-baserow.com/api)
BASEROW_BASE_URL=
BASEROW_API_TOKEN=
