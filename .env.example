# Example environment configuration for the Luna backend
#
# Copie para .env e preencha seus valores reais. Em produção (Railway),
# você pode definir as variáveis pelo painel do serviço.

# ——————————— UAZAPI (WhatsApp) ———————————
# Base URL da sua instância Uazapi (sem barra final)
# Exemplo: https://sua-instancia.uazapi.com
UAZAPI_BASE_URL=
# Token da INSTÂNCIA (não é o admin token)
UAZAPI_TOKEN=
# Endpoints opcionais (mantém padrão se usar a instalação padrão)
UAZAPI_SEND_TEXT_PATH=/send/text
UAZAPI_SEND_MEDIA_PATH=/send/media

# ——————————— Postgres ———————————
# Você pode colar aqui qualquer formato comum; o backend converte para o driver assíncrono:
# - postgres://user:pass@host:5432/dbname
# - postgresql://user:pass@host:5432/dbname
# - postgresql+psycopg2://user:pass@host:5432/dbname
# - postgresql+asyncpg://user:pass@host:5432/dbname
DATABASE_URL=
# SSL no asyncpg é configurado via connect_args (não via sslmode).
# 'require' (padrão em cloud/Railway) liga SSL; 'disable' para rodar local.
DB_SSLMODE=require

# ——————————— OpenAI ———————————
OPENAI_API_KEY=
ASSISTANT_ID=

# ——————————— Baserow (opcional para guardar arquivos) ———————————
# Cloud oficial: https://api.baserow.io
# Self-hosted: use a URL do seu servidor (ex.: https://meu-baserow.com/api)
BASEROW_BASE_URL=
BASEROW_API_TOKEN=
